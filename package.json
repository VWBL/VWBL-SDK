{
  "name": "vwbl-sdk.js",
  "description": "VWBL SDK for TypeScript",
  "version": "1.0.0",
  "private": "true",
  "repository": "https://github.com/VWBL/VWBL-SDK.git",
  "license": "MIT",
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": ["**"]
  },
  "scripts": {
    "format-all": "yarn workspace run lint --fix --quiet",
    "format:evm": "yarn workspace evm run lint --fix --quiet",
    "format:core": "yarn workspace vwbl-core run lint --fix --quiet",
    "lint-all": "yarn workspaces run lint",
    "lint:evm": "yarn workspace evm lint",
    "lint:core": "yarn workspace vwbl-core lint",
    "test-all": "yarn workspaces run test",
    "test:evm": "yarn workspace evm test"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.9.0",
    "@typescript-eslint/parser": "6.2.0",
    "eslint": "^8.10.0",
    "eslint-config-prettier": "^8.4.0",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-plugin-simple-import-sort": "^7.0.0",
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
