import type { Meta } from './Meta';
import type { xAppOttData } from './types';
export declare class xApp {
    private Meta;
    private userdata;
    constructor(MetaObject: Meta);
    get(ott: string): Promise<xAppOttData | null>;
}
