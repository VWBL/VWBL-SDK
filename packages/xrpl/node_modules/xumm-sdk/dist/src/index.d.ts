import { Meta } from './Meta';
import { Storage } from './Storage';
import { Payload } from './Payload';
import { xApp } from './xApp';
import { Push } from './Push';
import { JwtUserdata } from './JwtUserdata';
import type * as XummTypes from './types/xumm-api';
import type * as SdkTypes from './types/index';
import type { xAppOttData, UserTokenValidity, xAppJwtOtt } from './types/index';
declare class XummSdk {
    private Meta;
    storage: Storage;
    payload: Payload;
    jwtUserdata: JwtUserdata;
    Push: Push;
    xApp: xApp;
    constructor(apiKey?: string, apiSecret?: string);
    private getEnv;
    /**
     * Proxy methods to Meta class below
     */
    ping(): Promise<SdkTypes.ApplicationDetails>;
    getCuratedAssets(): Promise<SdkTypes.CuratedAssetsResponse>;
    getRails(): Promise<SdkTypes.Rails>;
    getHookHashes(): Promise<SdkTypes.HookHashes>;
    getHookHash(hookHash: string): Promise<SdkTypes.HookHash>;
    getRates(currencyCode: string): Promise<SdkTypes.RatesResponse>;
    getKycStatus(userTokenOrAccount: string): Promise<"NONE" | "IN_PROGRESS" | "REJECTED" | "SUCCESSFUL">;
    getTransaction(txHash: string): Promise<SdkTypes.XrplTransaction>;
    getNftokenDetail(tokenId: string): Promise<SdkTypes.NftokenDetail>;
    verifyUserTokens(userTokens: string[]): Promise<SdkTypes.UserTokenValidity[]>;
    verifyUserToken(token: string): Promise<UserTokenValidity | null>;
    setEndpoint(endpoint: string): boolean;
    caught(error: Error): void;
}
interface XummJwtOptionsStore {
    get: (ott: string) => xAppJwtOtt | void;
    set: (ott: string, ottData: xAppJwtOtt) => void;
}
export interface XummSdkJwtOptions {
    store?: XummJwtOptionsStore;
    fatalHandler?: (error: Error) => void;
    noAutoRetrieve?: boolean;
}
declare class XummSdkJwt extends XummSdk {
    private ottResolved;
    private jwt?;
    private resolve;
    private reject;
    fatalHandler?: (error: Error) => void;
    private store;
    constructor(apiKeyOrJwt: string, ott?: string, options?: XummSdkJwtOptions);
    _jwtStore(invoker: Meta, persistJwt: (jwt: string) => void): XummJwtOptionsStore;
    getOttData(): Promise<xAppOttData>;
    getJwt(): Promise<string | undefined>;
    caught(error: Error): void;
}
export { XummSdk, XummSdkJwt };
export type { XummTypes, SdkTypes, xAppOttData, UserTokenValidity, xAppJwtOtt, Storage, Payload, JwtUserdata, xApp, Push };
