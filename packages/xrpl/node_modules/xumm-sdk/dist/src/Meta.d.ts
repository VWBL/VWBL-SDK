import { XummSdk } from './index';
import type { ApplicationDetails, CreatePayload, AnyJson, CuratedAssetsResponse, PossibleKycStatuses, XrplTransaction, HookHash, HookHashes, Rails, NftokenDetail, RatesResponse, UserTokenValidity } from './types';
export declare class Meta {
    private apiKey;
    private apiSecret;
    private isBrowser;
    private jwtFlow;
    private jwt?;
    private injected;
    private invoker?;
    private authPromise?;
    private authPromiseResolve?;
    endpoint: string;
    constructor(apiKey: string, apiSecret: string);
    setEndpoint(endpoint: string): boolean;
    private authorize;
    call<T>(endpoint: string, httpMethod?: string, data?: CreatePayload | AnyJson): Promise<T> | never;
    ping(): Promise<ApplicationDetails>;
    getCuratedAssets(): Promise<CuratedAssetsResponse>;
    getRates(currencyCode: string): Promise<RatesResponse>;
    getKycStatus(userTokenOrAccount: string): Promise<keyof PossibleKycStatuses>;
    getRails(): Promise<Rails>;
    getHookHash(hookHash: string): Promise<HookHash>;
    getHookHashes(): Promise<HookHashes>;
    getTransaction(txHash: string): Promise<XrplTransaction>;
    getNftokenDetail(tokenId: string): Promise<NftokenDetail> | never;
    verifyUserTokens(userTokens: string[]): Promise<UserTokenValidity[]>;
    _inject(Invoker: XummSdk): void;
}
