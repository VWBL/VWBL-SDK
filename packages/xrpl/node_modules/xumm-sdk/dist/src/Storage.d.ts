import type { Meta } from './Meta';
import type { AnyJson } from './types';
export declare class Storage {
    private Meta;
    constructor(MetaObject: Meta);
    get(): Promise<AnyJson | null>;
    set(data?: AnyJson): Promise<boolean>;
    delete(): Promise<boolean>;
}
