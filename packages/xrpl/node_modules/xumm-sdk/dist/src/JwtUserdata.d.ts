import type { Meta } from './Meta';
import type { AnyJson } from './types';
export declare class JwtUserdata {
    private Meta;
    constructor(MetaObject: Meta);
    list(): Promise<string[]>;
    get(key: string | string[]): Promise<AnyJson>;
    delete(key: string): Promise<boolean>;
    set(key: string, data: AnyJson): Promise<boolean>;
}
