import type { Meta } from './Meta';
import type { EventResponse, PushResponse, EventPushPostBody } from './types';
export declare class Push {
    private Meta;
    private userdata;
    constructor(MetaObject: Meta);
    event(data: EventPushPostBody): Promise<EventResponse>;
    notification(data: EventPushPostBody): Promise<PushResponse>;
}
